<h2>ユーザー</h2>
  <% @users.each do |user| %>
    <%= link_to user.name, user_path(user) %>
    <%= user.email %>
    <%= render 'follow_form', user: user %>
  <% end %>
<hr>
<%= link_to '新規投稿', new_article_path, class: "btn btn-primary" %>

<%= link_to '投稿一覧', articles_path, class: "btn btn-primary" %>